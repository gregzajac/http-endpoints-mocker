version: '3.9'

services:

  mocker:
    build: ./mocker-service
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    volumes:
      - ./mocker-service/:/code
    ports:
      - 8001:8000

  # nginx:
  #   build: ./nginx
  #   ports:
  #     - 80:80
  #     - 443:443
  #   depends_on:
  #     - api

